# MongoDB Configuration - CRÍTICO PARA PRODUCCIÓN
# Reemplaza con tu verdadera MONGODB_URI de MongoDB Atlas
MONGODB_URI=mongodb+srv://staioirish_db_user:4vDAORWyykD9YaUF@arc001.1tlrpac.mongodb.net/arc_raiders?retryWrites=true&w=majority&appName=Arc001
DB_NAME=arc_raiders

# Server Configuration
PORT=3001
NODE_ENV=production

# JWT Authentication
# CRÍTICO: Genera un JWT_SECRET único y fuerte con:
# openssl rand -base64 64
JWT_SECRET=arc_raiders_prod_secret_key_generate_unique_value

# CORS Configuration
# URLs de producción (sin localhost)
CORS_ORIGINS=https://arc001.vercel.app,https://arc-raiders.vercel.app

# Rate Limiting (más estricto en producción)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50

# Notas importantes:
# 1. MONGODB_URI debe estar configurada en Render como variable de entorno
# 2. Asegúrate que la IP de Render esté autorizada en MongoDB Atlas
#    - Ve a MongoDB Atlas > Network Access
#    - Add Current IP Address o permite 0.0.0.0/0 (menos seguro pero funciona)
# 3. El usuario de MongoDB debe tener permisos de read/write en la base de datos
